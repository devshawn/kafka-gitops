settings:
  topics:
    blacklist:
      prefixed:
        - _schemas

users:
  test-user:
    principal: User:test

customUserAcls:
  test-user:
    read-all-kafka:
      name: kafka.
      type: UNKNOWN
      pattern: PREFIXED
      host: "*"
      operation: READ
      permission: ALLOW
